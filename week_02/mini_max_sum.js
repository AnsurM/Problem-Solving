// Complete the miniMaxSum function below.
function miniMaxSum(arr) {

    // create an array for holding sums of each index in the index which was skipped
    // loop over each index of input array
    // use reduce with index 
}
